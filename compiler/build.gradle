apply from: "$rootProject.projectDir/java.gradle"
apply from: "$rootProject.projectDir/maven-jar.gradle"

dependencies {
    include project(':external')
    compile project(':commons')
    testCompile 'junit:junit:4.12'
    testCompile project(':dummies')
}
